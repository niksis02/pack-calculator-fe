# Frontend environment variables (build-time, injected by Vite)

# Backend API base URL
# In development with the Vite proxy this defaults to /api/v1 (no need to set it)
VITE_API_BASE_URL=http://localhost:3000/api/v1
